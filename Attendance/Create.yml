info:
  name: Create
  type: http
  seq: 13

http:
  method: POST
  url: "{{baseUrl}}/attendance/create"
  body:
    type: json
    data: |-
      {
        "timeStamp": 1758432600004,
        "userId": "a09c2fe9-02a4-438a-88fa-76153fb4c7e5"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
