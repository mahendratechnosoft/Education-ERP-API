info:
  name: "{{baseUrl}}/uploads/{filename}"
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/uploads/{filename}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
