info:
  name: getAllDepartments
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/department/getAllDepartments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
