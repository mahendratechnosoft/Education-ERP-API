info:
  name: getAllExamBasicInfo
  type: http
  seq: 20

http:
  method: GET
  url: "{{baseUrl}}/exam/getAllExamBasicInfo?subjectId="
  params:
    - name: subjectId
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
