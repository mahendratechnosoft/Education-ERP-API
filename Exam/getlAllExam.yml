info:
  name: getlAllExam
  type: http
  seq: 19

http:
  method: PUT
  url: "{{baseUrl}}/exam/getAllExam"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
