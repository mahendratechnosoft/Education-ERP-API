info:
  name: getAllProgramBasicInfo
  type: http
  seq: 16

http:
  method: GET
  url: "{{baseUrl}}/program/getAllProgramBasicInfo"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
