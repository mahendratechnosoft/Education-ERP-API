info:
  name: getAllProgram
  type: http
  seq: 16

http:
  method: PUT
  url: "{{baseUrl}}/program/getAllProgram"
  body:
    type: json
    data: |-
      {
      "instituteId": "",
        "search": "",
       "from": "",
        "to": "",
       "creatorIds":[]

      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
