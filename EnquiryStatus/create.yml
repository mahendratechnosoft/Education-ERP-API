info:
  name: create
  type: http
  seq: 15

http:
  method: POST
  url: "{{baseUrl}}/enquiryStatus/create"
  body:
    type: json
    data: |-
      {
        "status":"New",
        "active":true
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
