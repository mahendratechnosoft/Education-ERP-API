info:
  name: getAllStatus
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}/enquiryStatus/getAllStatus?instituteId="
  params:
    - name: instituteId
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
