info:
  name: update
  type: http
  seq: 16

http:
  method: PUT
  url: "{{baseUrl}}/enquiryStatus/update"
  body:
    type: json
    data: |-
      {
        "id": "003fb035-8663-432d-9aaa-8720ac358c79",
        "status": "New 1",
        "active": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
