info:
  name: getAllSubject
  type: http
  seq: 23

http:
  method: PUT
  url: "{{baseUrl}}/subject/getAllSubject"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
