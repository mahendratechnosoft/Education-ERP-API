info:
  name: getAllSubjectBasicInfo
  type: http
  seq: 22

http:
  method: GET
  url: "{{baseUrl}}/subject/getAllSubjectBasicInfo"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
