info:
  name: updateOrganization
  type: http
  seq: 3

http:
  method: PUT
  url: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
