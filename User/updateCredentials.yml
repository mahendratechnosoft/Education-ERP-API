info:
  name: updateCredentials
  type: http
  seq: 1

http:
  method: PUT
  url: "{{baseUrl}}/user/updateCredentials"
  body:
    type: json
    data: |-
      {
        "userId": "9009b116-ac35-4671-85b4-1eb4595b1e50",
        "username": "INSTI1",
        "newPassword": "INSTI1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
