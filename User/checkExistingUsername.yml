info:
  name: checkExistingUsername
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/user/checkExistingUsername/{username}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
