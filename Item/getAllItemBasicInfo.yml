info:
  name: getAllItemBasicInfo
  type: http
  seq: 21

http:
  method: GET
  url: "{{baseUrl}}/item/getAllItemBasicInfo"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
