info:
  name: register
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/register
  body:
    type: json
    data: |-
      {
          "username": "root",
          "password": "root"

      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
