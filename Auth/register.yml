info:
  name: register
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/register"
  body:
    type: json
    data: |-
      {
          "username": "root",
          "password": "1234"

      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
