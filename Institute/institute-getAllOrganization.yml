info:
  name: institute/getAllOrganization
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseUrl}}/institute/getAllInstitute"
  body:
    type: json
    data: |-
      {
        "search": "",
        "creatorIds":[],
        "from": "",
        "to": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
