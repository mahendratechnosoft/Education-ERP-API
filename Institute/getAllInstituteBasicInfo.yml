info:
  name: getAllInstituteBasicInfo
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/institute/getAllInstituteBasicInfo"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
